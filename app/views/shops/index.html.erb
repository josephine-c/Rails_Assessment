<p id="notice"><%= notice %></p>

<div class="title-container">
<h1>Shops</h1>
</div>

<div class="shops">
  <% @shops.each do |shop| %>
    <div class="card">
      <div class="thumbnail">
        <div class="caption">
          <%# <%= image_tag shop.picture if shop.picture.attached? %>
          <h3><%= link_to shop.name, shop %></h3>
            <p><%= shop.description %></p>
              <table>
                <thead>
                  <tr>
                    <th colspan="3"></th>
                  </tr>
                </thead>

                <tbody>
                      <tr>
                        <td><%= link_to 'Show Products', shop, class: "btn btn-default" %></td>
                        <%# <td><%= link_to 'Edit', edit_shop_path(shop) %>
                        <%# <td><%= link_to 'Destroy', shop, method: :delete, data: { confirm: 'Are you sure?' } %>
                      </tr>
                </tbody>
              </table>
        </div>
      </div>
    </div>
</div>
  <% end %>
<br>

<%# <%= link_to 'New Shop', new_shop_path %>
