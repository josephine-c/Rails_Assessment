<p id="notice"><%= notice %></p>

<h1>Shops</h1>

<% @shops.each do |shop| %>
    <div class="card">
    <%# <%= image_tag shop.picture if shop.picture.attached? %>
    <p><%= shop.name %></p>
          <p><%= shop.description %></p>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
        <tr>
          <td><%= link_to 'Show Products', shop %></td>
          <%# <td><%= link_to 'Edit', edit_shop_path(shop) %>
          <%# <td><%= link_to 'Destroy', shop, method: :delete, data: { confirm: 'Are you sure?' } %>
        </tr>
  </tbody>
</table>
 </div>
  <% end %>
<br>

<%= link_to 'New Shop', new_shop_path %>
